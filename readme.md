# Broken Auth Assignment

Steps performed:

1. Login → generated loginSessionId
2. Verify OTP → session cookie created
3. Token generation → JWT token issued
4. Access protected route → success_flag retrieved